# ---------------------------------------------------------
# Escolhe UM dos provedores abaixo e preenche a respetiva chave
# ---------------------------------------------------------

# Opção A: OpenAI
OPENAI_API_KEY=sk-...
MODEL_NAME=gpt-4o-mini

# Opção B: Anthropic (Claude)
# ANTHROPIC_API_KEY=sk-ant-...
# MODEL_NAME=claude-sonnet-4-6

# Porta do servidor Flask (opcional)
PORT=5000

# ---------------------------------------------------------
# Geração de imagens (AUTOMATIC1111 / Stable Diffusion local)
# ---------------------------------------------------------
# URL onde o AUTOMATIC1111 está a correr (iniciado com a flag --api)
SD_API_URL=http://127.0.0.1:7860

# ---------------------------------------------------------
# OCR de anexos (Tesseract) — usado para juntar imagens num PDF pesquisável
# ---------------------------------------------------------
# No Windows, se o tesseract.exe não estiver no PATH, indica o caminho completo, ex:
# TESSERACT_CMD=C:\Program Files\Tesseract-OCR\tesseract.exe

# Idiomas usados no OCR (códigos do Tesseract, separados por "+")
OCR_LANGUAGES=por+eng
